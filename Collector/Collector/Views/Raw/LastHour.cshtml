@model Collector.Models.ViewModels.Raw.LastHourRawViewModel

@{
    ViewData["Title"] = "Last Hour Raw Events";
}

<h1>Last Hour - Raw Items</h1>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Raw Json Report</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.RawTelemetry)
        {
            <tr>
                <td>@item</td>
            </tr>
        }
    </tbody>
</table>